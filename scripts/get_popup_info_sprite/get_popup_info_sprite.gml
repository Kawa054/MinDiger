function get_popup_info_sprite(info_type){
	if(info_type == POPUP_INFOTYPE_CARE){ return icon_exclamation; }
	if(info_type == POPUP_INFOTYPE_COPY){ return icon_ex_duplicate; }
	if(info_type == POPUP_INFOTYPE_EXIT){ return icon_door; }
	if(info_type == POPUP_INFOTYPE_INFO){ return icon_info; }
}